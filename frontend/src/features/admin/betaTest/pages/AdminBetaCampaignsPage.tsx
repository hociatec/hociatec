import { useEffect, useState } from 'react';
import { fetchAdminCampaigns, createAdminCampaign, type AdminCampaignDto } from '../api';
import { PageContainer } from '@/shared/components/PageContainer';
export const AdminBetaCampaignsPage = () => { const [items,setItems]=useState<AdminCampaignDto[]>([]); const [form,setForm]=useState({name:'',description:'',status:'draft'}); const load=()=>void fetchAdminCampaigns().then(setItems); useEffect(load,[]); const submit=async(e:React.FormEvent)=>{e.preventDefault();await createAdminCampaign(form);setForm({name:'',description:'',status:'draft'});load();}; return <PageContainer size="admin" title="Campagnes bêta"><form onSubmit={submit} className="mb-8 max-w-xl space-y-3 rounded border bg-white p-5"><h2 className="text-lg font-semibold">Nouvelle campagne</h2><input className="w-full rounded border p-3" placeholder="Nom" value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/><textarea className="w-full rounded border p-3" placeholder="Description" value={form.description} onChange={e=>setForm({...form,description:e.target.value})} required/><select className="w-full rounded border p-3" value={form.status} onChange={e=>setForm({...form,status:e.target.value})}><option value="draft">Brouillon</option><option value="active">Active</option><option value="closed">Clôturée</option></select><button className="rounded bg-brand-700 px-4 py-2 font-semibold text-white">Créer la campagne</button></form><div className="space-y-3">{items.map(item=><article key={item.id} className="rounded border bg-white p-4"><strong>{item.name}</strong><span className="ml-3 text-sm text-stone-500">{item.status}</span><p className="mt-1 text-sm text-stone-600">{item.description}</p></article>)}</div></PageContainer>; };
