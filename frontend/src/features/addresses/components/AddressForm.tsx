import type { Dispatch, SetStateAction } from 'react';
import { AddressFields } from './AddressFields';
import type { AddressFormState } from '@/features/addresses/types/address';

export const AddressForm = ({ form, saving, setForm, onSubmit }: { form: AddressFormState; saving: boolean; setForm: Dispatch<SetStateAction<AddressFormState>>; onSubmit: () => void }) => <section className="rounded-xl border border-brand-100 bg-white p-6 shadow-sm"><div className="mb-5"><h2 className="text-xl font-semibold text-brand-900">Ajouter une adresse</h2><p className="mt-1 text-sm text-stone-600">Renseignez une adresse claire pour faciliter la préparation, la livraison et la facturation.</p></div><div className="grid gap-4"><AddressFields form={form} setForm={setForm} /><label className="flex items-center gap-3 rounded-2xl border border-brand-100 bg-brand-50 px-4 py-3 text-sm text-stone-700"><input type="checkbox" checked={Boolean(form.isDefault)} onChange={(event) => setForm((current) => ({ ...current, isDefault: event.target.checked }))} />Définir comme adresse par défaut</label><button type="button" className="address-submit" onClick={onSubmit} disabled={saving}>{saving ? 'Ajout en cours...' : 'Ajouter l’adresse'}</button></div></section>;
