framework:
    rate_limiter:
        public_api:
            policy: 'sliding_window'
            limit: 60
            interval: '1 minute'
            # identify by IP by default
        checkout:
            policy: 'fixed_window'
            limit: 10
            interval: '10 minutes'
        activation_verify:
            policy: 'fixed_window'
            limit: 5
            interval: '1 minute'
        password_reset_request:
            policy: 'sliding_window'
            limit: 5
            interval: '15 minutes'
        password_reset_confirm:
            policy: 'fixed_window'
            limit: 10
            interval: '15 minutes'
        auth_register:
            policy: 'sliding_window'
            limit: 5
            interval: '1 hour'
        auth_refresh:
            policy: 'sliding_window'
            limit: 20
            interval: '1 minute'
        contact_public:
            policy: 'sliding_window'
            limit: 5
            interval: '1 minute'
        product_share_public:
            policy: 'sliding_window'
            limit: 10
            interval: '1 minute'
        content_share_public:
            policy: 'sliding_window'
            limit: 10
            interval: '1 minute'
        beta_signup:
            policy: 'sliding_window'
            limit: 5
            interval: '1 hour'
        beta_report_create:
            policy: 'sliding_window'
            limit: 12
            interval: '1 hour'
        beta_report_comment:
            policy: 'sliding_window'
            limit: 20
            interval: '1 minute'
